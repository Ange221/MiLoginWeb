{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd MiLoginWeb && ./gradlew bootJar --no-daemon",
    "startCommand": "java -jar MiLoginWeb/build/libs/miloginweb-0.0.1-SNAPSHOT.jar"
  }
}
